name: architecture-guard

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  import-direction:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.work

      - name: Run architecture guard
        shell: pwsh
        run: pwsh ./harness/testing/arch_guard.ps1

  root-validation:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.work

      - name: Run go vet
        shell: pwsh
        run: go vet ./...

      - name: Run root tests
        shell: pwsh
        run: go test -race -count=1 ./...

  example-validation:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.work

      - name: Validate example modules
        shell: pwsh
        run: pwsh ./harness/testing/validate_examples.ps1
