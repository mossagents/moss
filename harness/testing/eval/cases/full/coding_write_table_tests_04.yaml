id: coding-write-table-tests-04
description: Ensure agent adds table-driven tests for changed behavior
tags: [core, coding, full]

input:
  messages:
    - role: user
      content: "请补充表驱动测试覆盖边界条件"
  tools: [read_file, insert_edit_into_file]

expect:
  contains: ["测试", "边界"]
  max_steps: 12

scoring:
  pass_threshold: 0.8

